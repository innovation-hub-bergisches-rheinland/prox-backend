spring:
  application:
    name: prox-backend # Ensure the correct application displayName as it is used for the kubernetes resource resolution
  cloud:
    kubernetes:
      config:
        enabled: true